# Category Guide Route

Canonical source: `prompts/playbook.md`.

Read these sections before classifying transactions or calibrating confidence:

- "流程 A：處理一張新帳單（月度匯入）"
- "A3. 未覆蓋 → 分類 + 信心度 + websearch（核心）"
- "標準 14 類"
- "附錄二：資料模型重點"

At runtime, also read `GET /api/meta` and use the app's current category metadata when available.

Do not keep a parallel category list here. If category names or boundaries change, update `prompts/playbook.md` and the app constants together.
